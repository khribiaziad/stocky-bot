services:
  - type: web
    name: stocky-bot
    runtime: node
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: NODE_VERSION
        value: 20.0.0
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: BOT_SECRET
        sync: false
